name: Future DPopCleaner build (disabled)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic 0.2.15 build is disabled until the reconstructed source is verified. Current downloadable release is 0.2.14 BETA."
