name: Experimental DPopCleaner rebuild (DISABLED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Rebuild intentionally disabled
        run: |
          echo "The reconstructed 0.3.1 build has been disabled."
          echo "The public site is pinned to the original working DPopCleaner 0.2.14 installer."
